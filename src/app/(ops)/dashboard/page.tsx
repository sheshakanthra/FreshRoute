import { RoutePlaceholder } from "@/components/shared/RoutePlaceholder";

export default function DashboardPage() {
  return (
    <RoutePlaceholder
      title="Operations Center"
      description="FreshRoute decision state across active perishable batches."
      builtInSession="Populated in Session 3 — KPI grid and active batch table."
    />
  );
}
